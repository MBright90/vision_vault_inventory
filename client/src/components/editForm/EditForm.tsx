import React from "react";

import style from './EditForm.module.scss';

const EditForm: React.FC = () => {
    return (
        <main className={style.editFormContainer}>
            <p>This is the editing form</p>
        </main>
    );
};

export default EditForm;
