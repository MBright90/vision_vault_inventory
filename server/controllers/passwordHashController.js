/* eslint-disable camelcase */
const PasswordHash = require('../models/passwordHash');

async function get_password_hash() {
  // get current hash of password
}

async function update_password_hash() {
  // update currently stored hash
}

module.exports = {
  get_password_hash,
  update_password_hash,
};
